services:
  backend:
    build:
      context: ./backend
    environment:
      WORKSPACE_PATH: /app/workspace
      DATABASE_URL: sqlite:////app/workspace/database/research_graph.db
      CORS_ORIGINS: ${CORS_ORIGINS:-https://ugraph.me,https://www.ugraph.me,http://ugraph.me,http://www.ugraph.me}
    volumes:
      - ./workspace:/app/workspace
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      - backend
    restart: unless-stopped
